<footer class="site-footer">
  <div class="container footer-grid">
    <div class="footer-brand">
      <img src="/images/buildscope-logo.webp" alt="工程白話文 BuildScope 完整標誌" width="98" height="98" />
      <div><strong>工程白話文</strong><span>用白話拆解建築工程知識</span><a href="https://www.instagram.com/build_scope/" target="_blank" rel="noreferrer">Instagram · @build_scope</a></div>
    </div>
    <div><h3>內容</h3><a href="/blog/">工程 Blog</a><a href="/news/">工程新聞</a><a href="/templates/">文件範本</a><a href="/ebooks/">電子書</a></div>
    <div><h3>工具</h3><a href="https://buildscope-dictionary.com/">工程字典</a><a href="/ai-tools/">AI 工程工具</a><a href="/about/">關於我們</a></div>
    <div class="newsletter"><h3>訂閱工程電子報</h3><p>每週精選工程知識、新聞及實用工具。</p><form><label class="visually-hidden" for="footer-email">電郵地址</label><input id="footer-email" type="email" placeholder="輸入電郵地址" required /><button type="submit">訂閱</button></form><small class="form-message" aria-live="polite"></small></div>
  </div>
  <div class="container copyright">© 2026 工程白話文 BuildScope．保留所有權利。</div>
</footer>

<script>
  const form = document.querySelector<HTMLFormElement>(".newsletter form");
  form?.addEventListener("submit", (event) => {
    event.preventDefault();
    const message = form.parentElement?.querySelector<HTMLElement>(".form-message");
    if (message) message.textContent = "多謝訂閱！正式連接電郵系統後即可接收更新。";
    form.reset();
  });
</script>

<style>
  .site-footer { padding-top: 56px; border-top: 1px solid var(--line); background: var(--white); }
  .footer-grid { display: grid; grid-template-columns: 1.7fr .7fr .7fr 1.4fr; gap: 44px; }
  .footer-brand { display: flex; align-items: flex-start; gap: 16px; }
  .footer-brand img { width: 94px; height: 94px; object-fit: contain; }
  .footer-brand div { display: grid; }
  .footer-brand strong { font-size: 1.15rem; }
  .footer-brand span, .footer-brand a, .site-footer p { color: var(--body); font-size: .88rem; }
  .footer-brand a { margin-top: 10px; color: var(--orange-dark); font-weight: 800; }
  .site-footer h3 { margin: 0 0 12px; font-size: .9rem; }
  .site-footer > .footer-grid > div:not(:first-child) { display: flex; flex-direction: column; align-items: flex-start; }
  .site-footer > .footer-grid > div > a { margin-bottom: 7px; color: var(--body); font-size: .88rem; }
  .site-footer > .footer-grid > div > a:hover { color: var(--orange-dark); }
  .newsletter p { margin: 0 0 12px; }
  .newsletter form { display: flex; width: 100%; }
  .newsletter input { min-width: 0; flex: 1; min-height: 44px; padding: 0 12px; border: 1px solid var(--line); border-right: 0; border-radius: 5px 0 0 5px; outline: none; }
  .newsletter input:focus { border-color: var(--orange); }
  .newsletter button { min-height: 44px; padding: 0 16px; border: 0; border-radius: 0 5px 5px 0; color: white; background: var(--orange); font-weight: 800; cursor: pointer; }
  .form-message { display: block; margin-top: 8px; color: var(--success); }
  .copyright { margin-top: 42px; padding: 20px 0; border-top: 1px solid var(--line); color: var(--muted); text-align: center; font-size: .78rem; }
  @media (max-width: 900px) { .footer-grid { grid-template-columns: 1.3fr 1fr 1fr; } .newsletter { grid-column: 1 / -1; } }
  @media (max-width: 580px) { .footer-grid { grid-template-columns: 1fr 1fr; gap: 32px 24px; } .footer-brand { grid-column: 1 / -1; } }
</style>
